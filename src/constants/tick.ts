/** In ms */
export const tickDuration = 200;

/** The minimum duration for an event that we don't care about the duration for, or doesn't really have a duration (e.g. instant damage, e.g. Rei's homing arrow) */
export const minEventDuration = tickDuration;
